kind = "mobile"
previewPath = "/"
title = "SignBridge"
version = "1.0.0"
id = "artifacts/signbridge"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 19202

[services.development]
run = "pnpm --filter @workspace/signbridge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/signbridge", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/signbridge", "run", "serve" ]

[services.env]
PORT = "19202"
BASE_PATH = "/"
